cd $HOME/.dot && git submodule init && git submodule update
cd $HOME/.dot/tmux && git submodule init && git submodule update
#cd $HOME/.dot/tmux/vendor/tmux-mem-cpu-load && cmake . && make && sudo make install
